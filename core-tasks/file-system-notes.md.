# file system navigation notes 

Here are the command i practiced in killercoda:

* `ls` : used to list the content current directory
* `ls -lah`: Used this to see all files in a list.
* `cd /var/log`: Used this to move into the log folder.
* `pwd`: Used this to make sure I wasn't lost!
* `cat` : used to exacute the content of file 
